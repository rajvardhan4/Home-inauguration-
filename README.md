# Home Inauguration Invitation

Open `index.html` directly, or run `npx serve .` inside this folder. Edit the family name, date, timings and address in `index.html`; update the countdown date in `script.js`.
